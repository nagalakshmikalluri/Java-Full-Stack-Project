# MySQL datasource
spring.datasource.url=jdbc:mysql://localhost:3306/jobportal?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=neelu@Y21CSM019
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# Explicit dialect (helps when JDBC metadata can't be read yet)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# JWT (example values, change secret to a strong value)
jwt.secret=very_secret_key_change_me
jwt.expiration=3600000

# Server (optional)
server.port=8080
